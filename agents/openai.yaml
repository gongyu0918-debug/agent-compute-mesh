interface:
  display_name: "Agent Travel"
  short_description: "Background research with isolated advisory hints / 隔离式背景研究提示"
  default_prompt: "Use $agent-travel to research an unresolved issue in the background and save only cross-validated advisory hints for a future task. / 用 $agent-travel 在后台研究未解决问题，只保存经过交叉验证、供后续任务参考的提示建议。"

policy:
  allow_implicit_invocation: true
